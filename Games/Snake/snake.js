class Snake {
    constructor() {
        this.snakeArray = [{x: 10, y: 10}];
        this.dx = 1;
        this.dy = 0;
    }

    draw() {
        // Drawing code for the snake.
    }

    update() {
        // Update the snake's position.
    }

    eat(food) {
        // Check if the snake has eaten the food.
    }
}
